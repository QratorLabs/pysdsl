kind: pipeline
name: default

steps:
- name: test-wheel
  image: debian:testing
  volumes:
  - name: cache
    path: /tmp/cache
  commands:
  - apt-get update
  - apt-get install -y python python-pip
  # - pip install $(ls -d /tmp/cache/dist/*)
  - pip install https://github.com/QratorLabs/pysdsl/releases/download/1.0.0a0/pysdsl-1.0.0a0-cp27-cp27mu-linux_x86_64.whl
  - mkdir /tmp/test
  - cd /tmp/test
  - python -c 'import pysdsl'

- name: build
  image: ivigns/nice-ubuntu:0.0.1
  volumes:
  - name: cache
    path: /tmp/cache
  commands:
  - git submodule sync
  - git submodule update --init --recursive
  - cd /drone/src/libdivsufsort
  - git pull origin master
  - cd /drone/src/sdsl-lite
  - git pull origin master
  - cd /drone/src/sdsl-lite/external/libdivsufsort
  - git pull origin master
  - cd /drone/src/pybind11
  - git pull origin master
  - cd /tmp
  - mkdir sdsl-lite-build
  - mkdir libdivsufsort-build
  - export CC=clang
  - export CXX=clang++
  - export CFLAGS="-O3 -pipe -fcolor-diagnostics -static-libgcc -fPIC -lpthread -Wl,-Bstatic -Wl,-Bdynamic"
  - export CXXFLAGS="-O3 -pipe -fcolor-diagnostics -static-libgcc -fPIC -lpthread -Wl,-Bstatic -Wl,-Bdynamic"
  - cd /tmp/libdivsufsort-build
  - cmake /drone/src/libdivsufsort
  - make install
  - cd /tmp/sdsl-lite-build
  - cmake /drone/src/sdsl-lite
  - make install
  - cd /drone/src/pybind11
  - pip install --no-binary ':all:' --no-cache-dir . --verbose
  - cd /drone/src
  - pip wheel --no-binary ':all:' --no-cache-dir -w dist . --verbose
  - cp -r . /tmp/cache

- name: show
  image: ivigns/nice-ubuntu:0.0.1
  volumes:
  - name: cache
    path: /tmp/cache
  commands:
  - cd /tmp/cache && ls -aR

- name: publish
  image: plugins/github-release
  volumes:
  - name: cache
    path: /tmp/cache
  settings:
    api_key:
      from_secret: repo_token
    files:
      - /tmp/cache/dist/*
    title: ${DRONE_TAG}
  when:
    event: tag

volumes:
- name: cache
  temp: {}
