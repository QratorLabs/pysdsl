FROM debian:testing
RUN mkdir /work
WORKDIR /work
RUN \
  apt update && \
  apt install -y --no-install-recommends clang cmake python-pip ssh git tar zip ca-certificates && \
  rm -rf /var/lib/apt/lists/*
