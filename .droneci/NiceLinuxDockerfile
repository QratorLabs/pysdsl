FROM debian:testing
RUN mkdir /work
WORKDIR /work
RUN \
  apt update && \
  apt install -y --no-install-recommends clang python-pip ssh git tar zip ca-certificates && \
  apt install -y cmake && \
  rm -rf /var/lib/apt/lists/*
